# frozen_string_literal: true

# File original exported from 18xx-maker/export-rb
# https://github.com/18xx-maker/export-rb
# rubocop:disable Layout/LineLength, Style/AsciiComments

module Engine
  module Config
    module Tile
      WHITE = {
        'blank' => '',
        'city' => 'city=revenue:0',
        'town' => 'town=revenue:0',
      }.freeze

      BLUE = {}.freeze
      RED = {}.freeze

      {{#each colors}}
      {{{name}}} = {
        {{#each tiles}}
        {{#if broken}}# {{/if}}'{{{id}}}' => '{{{encoding}}}',
        {{/each}}
      }.freeze

      {{/each}}
      COLORS = %i[white {{{symbols}}} red].freeze
    end
  end
end

# rubocop:enable Layout/LineLength, Style/AsciiComments
